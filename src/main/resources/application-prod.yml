spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://plottingdb.chi0ouqe0mjm.ap-northeast-2.rds.amazonaws.com:3306/plottingdb
    username: admin
    password: plower1234!!

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false

server:
  env: local