spring:
  profiles:
    active: test

  datasource:
    url: jdbc:h2:mem:plottingdb;MODE=MySQL;
    username: sa
    password: test
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        show_sql: true

server:
  env: test